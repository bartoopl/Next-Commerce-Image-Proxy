# Required: secret for HMAC URL signing (min 32 chars, use openssl rand -hex 32)
IMAGE_PROXY_SECRET=your-secret-key-min-32-characters

# Optional: allowlist of source hostnames (comma-separated). Empty = allow all (use with caution).
# IMAGE_PROXY_ALLOWED_ORIGINS=https://legacy-erp.example.com,https://pim.example.com

# Optional: max width in pixels (default 4096)
# IMAGE_PROXY_MAX_WIDTH=4096

# Optional: cache TTL in seconds for Cache-Control (default 31536000 = 1 year)
# IMAGE_PROXY_CACHE_MAX_AGE=31536000
